import React from 'react';
import { View } from 'react-native';

// This is just a placeholder screen that won't be shown
// because we're handling the QR code with a popup
export default function QRCodeScreen() {
  return <View />;
}